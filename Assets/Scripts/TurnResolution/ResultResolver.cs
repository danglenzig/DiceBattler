using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Rendering;

namespace Dice
{

    public sealed class CombatResolver
    {
        /*
        private readonly IResolverRules _rules;

        public CombatResolver(IResolverRules rules)
        {
            _rules = rules;
        }

        
        public TurnResolution Resolve(TableauResult tableauResult)
        {
            //return _rules.GetOutcome(tableauResult.RollResult, tableauResult.PlayerData, tableauResult.OpponentData);
        }
        

        public string SayHello()
        {
            return $"Combat resolver says hi!\n{_rules.SayHello()}";
        }
        */
    }
}

